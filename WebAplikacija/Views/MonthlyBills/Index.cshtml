@model List<WebAplikacija.ViewModels.MonthlyBillViewModel>

@{
    ViewBag.Title = "Mėnesio sąskaitos";
}

<h2>Mėnesio sąskaitos</h2>

<p>
    @Html.ActionLink("Sukurti nauja sąskaitą", "Create")
</p>
<table class="table table-striped table-hover" >
    <tr>
        <th>
            Sąskaitos aprašymas
        </th>       
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.BillDescription)
            </td>
            <td>
                <a href="@Url.Action("Edit", new { id = item.BillId })" class="btn badge">Koreguoti</a>
                <a href="@Url.Action("Details", new { id = item.BillId })" class="btn badge">Detaliau</a>
                <a href="@Url.Action("Delete", new { id = item.BillId })" class="btn badge">&times;</a>
            </td>
        </tr>
    }
</table>
